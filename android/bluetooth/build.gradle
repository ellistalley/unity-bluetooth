plugins {
    id("com.android.library")
}

android {
    compileSdk 30

    defaultConfig {
        minSdk 19
        targetSdk 19
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    compileOnly(files("/Applications/Unity/Hub/Editor/2020.3.8f1/PlaybackEngines/AndroidPlayer/Variations/mono/Development/Classes/classes.jar"))
}